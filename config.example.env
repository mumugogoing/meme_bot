# Example Trading Bot Configuration
# This file shows different configuration scenarios

# ============================================
# SCENARIO 1: Dry-Run Testing (Recommended First)
# ============================================
# Use this configuration to test the bot without executing real trades
# Run for at least 24 hours to understand behavior

DRY_RUN=true
AUTO_EXECUTE=false

# Use free public RPC endpoints for testing
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
BASE_RPC_URL=https://mainnet.base.org

# Strategy settings - moderate threshold
WIN_PROBABILITY_THRESHOLD=0.80
MIN_VOLUME_DEX=10000.0
MIN_LIQUIDITY=5000.0
MAX_HONEYPOT_SCORE=0.2

# Conservative risk settings
SINGLE_POSITION_PCT=0.01
TOTAL_EXPOSURE_PCT=0.05
DAILY_LOSS_LIMIT=500.0
ACCOUNT_BALANCE=10000.0

# ============================================
# SCENARIO 2: Testnet Testing
# ============================================
# Test with testnet tokens before using real money

# DRY_RUN=false
# AUTO_EXECUTE=true

# SOLANA_RPC_URL=https://api.devnet.solana.com
# SOLANA_WS_URL=wss://api.devnet.solana.com
# BASE_RPC_URL=https://goerli.base.org
# BASE_WS_URL=wss://goerli.base.org

# ACCOUNT_BALANCE=100.0
# DAILY_LOSS_LIMIT=10.0

# ============================================
# SCENARIO 3: Small Amount Production
# ============================================
# First production run with minimal risk

# DRY_RUN=false
# AUTO_EXECUTE=true

# Use your private RPC endpoints (recommended for production)
# SOLANA_RPC_URL=https://your-private-solana-node.com
# BASE_RPC_URL=https://your-private-base-node.com

# Very conservative for first run
# ACCOUNT_BALANCE=100.0              # $100 only
# SINGLE_POSITION_PCT=0.05           # 5% = $5 per trade
# TOTAL_EXPOSURE_PCT=0.10            # 10% = $10 total
# DAILY_LOSS_LIMIT=10.0              # $10 max loss per day

# Higher threshold for safety
# WIN_PROBABILITY_THRESHOLD=0.85     # 85% minimum
# MAX_HONEYPOT_SCORE=0.15            # Stricter honeypot check

# ============================================
# SCENARIO 4: Aggressive Strategy (Experienced Only)
# ============================================
# Higher risk, higher potential reward
# ONLY use after extensive testing!

# DRY_RUN=false
# AUTO_EXECUTE=true

# ACCOUNT_BALANCE=5000.0
# SINGLE_POSITION_PCT=0.02           # 2% = $100 per trade
# TOTAL_EXPOSURE_PCT=0.08            # 8% = $400 total
# DAILY_LOSS_LIMIT=200.0

# WIN_PROBABILITY_THRESHOLD=0.75     # Lower threshold = more trades
# MIN_VOLUME_DEX=5000.0              # Lower volume requirement
# MIN_LIQUIDITY=2000.0

# ============================================
# SCENARIO 5: Conservative High-Value
# ============================================
# Larger balance but very conservative strategy

# DRY_RUN=false
# AUTO_EXECUTE=true

# ACCOUNT_BALANCE=50000.0
# SINGLE_POSITION_PCT=0.005          # 0.5% = $250 per trade
# TOTAL_EXPOSURE_PCT=0.03            # 3% = $1,500 total
# DAILY_LOSS_LIMIT=500.0

# WIN_PROBABILITY_THRESHOLD=0.90     # Very high threshold
# MAX_HONEYPOT_SCORE=0.10            # Very strict
# MIN_VOLUME_DEX=50000.0             # High volume only

# ============================================
# API Keys (Optional but Recommended)
# ============================================

# CoinGecko for price and volume data
# COINGECKO_API_KEY=your_api_key_here

# OKX for CEX volume and price
# OKX_API_KEY=your_api_key
# OKX_API_SECRET=your_api_secret
# OKX_API_PASSPHRASE=your_passphrase

# Twitter for social signals
# TWITTER_API_KEY=your_twitter_api_key

# ============================================
# Wallet Configuration
# ============================================

# Option 1: OKX Wallet SDK (Recommended)
USE_OKX_WALLET=true

# Option 2: Private Key (Use with extreme caution!)
# USE_OKX_WALLET=false
# PRIVATE_KEY=0x...
# 
# WARNING: NEVER commit your private key to version control!
# Use environment variables or secrets management (KMS, Vault)

# ============================================
# Advanced Settings
# ============================================

# Scan intervals (seconds)
SCAN_INTERVAL_SOLANA_SEC=2
SCAN_INTERVAL_BASE_SEC=2

# Execution settings
MAX_SIMULATE_RETRIES=3
SIMULATE_TIMEOUT_SEC=30
CONFIRMATIONS_WAIT=2
DEFAULT_TIME_WINDOW_MIN=15

# Slippage tolerance
MAX_SLIPPAGE=0.05                  # 5% max slippage

# Database (default: SQLite in current directory)
DATABASE_URL=sqlite://./meme_bot.db

# Monitoring
PROMETHEUS_PORT=9090
LOG_LEVEL=info                     # debug, info, warn, error

# ============================================
# Blacklist/Whitelist
# ============================================

# Known scam tokens (comma-separated)
# BLACKLISTED_TOKENS=0xScamToken1,0xScamToken2

# Known scam creators
# BLACKLISTED_CREATORS=SomeScammerAddress

# Trusted tokens (will be marked high priority)
# WHITELISTED_TOKENS=

# ============================================
# Notes and Tips
# ============================================

# 1. Always start with DRY_RUN=true
# 2. Test on testnet before mainnet
# 3. Start with small amounts
# 4. Monitor closely for first 24-48 hours
# 5. Adjust thresholds based on results
# 6. Keep private keys secure (use KMS in production)
# 7. Set up monitoring and alerts
# 8. Review metrics regularly via API
# 9. Have an emergency stop procedure
# 10. Only trade what you can afford to lose!

# ============================================
# Risk Management Guidelines
# ============================================

# Beginner:
# - ACCOUNT_BALANCE: $100-500
# - SINGLE_POSITION_PCT: 0.01-0.02 (1-2%)
# - WIN_PROBABILITY_THRESHOLD: 0.85-0.90

# Intermediate:
# - ACCOUNT_BALANCE: $500-5000
# - SINGLE_POSITION_PCT: 0.02-0.03 (2-3%)
# - WIN_PROBABILITY_THRESHOLD: 0.80-0.85

# Advanced (not recommended):
# - ACCOUNT_BALANCE: $5000+
# - SINGLE_POSITION_PCT: 0.01-0.02 (even with larger balance)
# - WIN_PROBABILITY_THRESHOLD: 0.75-0.80

# Remember: Lower position sizes and higher thresholds = safer but fewer trades
